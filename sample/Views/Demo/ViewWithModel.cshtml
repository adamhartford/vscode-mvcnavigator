@model dynamic

@{
    ViewData["Title"] = "View with Model";
}

<h2>View with Model</h2>
<p>This view demonstrates View() navigation with model objects.</p>
<p>You should have been able to Ctrl+click on "View" (the method name) in the DemoController.ViewWithModel action to navigate here.</p>

@if (Model != null)
{
    <p>Model Name: @Model.Name</p>
}
